version: "3.3"
services:
  db:
    image: kartoza/postgis:11.5-2.5
    restart: always
    environment:
      - POSTGRES_DB=fieldfresh
      - POSTGRES_USER=root
      - POSTGRES_PASSWORD=pass
    expose:
      - "8888"
    ports:
      - "8888:5432"
